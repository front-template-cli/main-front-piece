import { ActionContext } from 'vuex'

/**
 * 组件持久化
 */
interface EditorState {}
const state: EditorState = {}
const getters = {}
const mutations = {}
const actions = {}

export default {
  actions,
  getters,
  mutations,
  state
}
